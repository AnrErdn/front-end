import Image from "next/image";
import Link from "next/link";

interface MovieType {
  id: number;
  title: string;
  backdrop_path: string;
  poster_path: string;
}

export default function Home() {
    return (
        <div>
            <header className="flex justify-between items-center p-6 bg-black bg-opacity-75">
            
            </header>
        </div>
    );
}